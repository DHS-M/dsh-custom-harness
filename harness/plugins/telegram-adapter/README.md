# Telegram adapter

Sidecar bot bridge for DSH (DM, groups @mention, topics, channels).

```bash
export TELEGRAM_BOT_TOKEN=...
export DSH_URL=https://your-dsh-host
node index.mjs
```

See `docs/changes/006-telegram-adapter.md`.
