# 010 — Railway template variable descriptions

## Problem
Template UI requires descriptions; GraphQL cannot store them on project variables.

## Solution
Document exact text in `deploy/railway-template-vars.json` for paste into the template editor.
