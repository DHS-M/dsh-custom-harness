# 008 — Render blueprint

## Problem
Need one-click cloud deploy options.

## Solution
`deploy/render.yaml` + `scripts/render-start.sh`; Deploy to Render button pattern documented at org root when needed.
