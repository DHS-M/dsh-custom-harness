# DHS-M custom overlays

| Path | Role |
|------|------|
| `plugins/` | `@custom/*` Cordis plugins |
| `scripts/` | railway-start, trust-all patch |
| `deploy/` | railway.toml, render.yaml, template vars |

Upstream source is **`../src`** (not this folder).
